{
  "name": "blogs-frontend",
  "private": true,
  "version": "5.0.0",
  "devDependencies": {
    "del": "^3.0.0",
    "gulp": "^3.9.1",
    "gulp-autoprefixer": "^4.0.0",
    "gulp-sass": "^3.1.0",
    "gulp-sass-error": "^1.0.5",
    "sass-lint": "^1.12.1",
    "sass-mq": "^3.3.2"
  },
  "scripts": {
    "gulp": "gulp default",
    "gulp-ci": "gulp distribution",
    "lint": "node_modules/.bin/sass-lint --no-exit -v",
    "watch": "gulp watch",
    "postinstall": "gulp"
  }
}
