<div class="component">
    <p class="inline">{{ tr('header_tag_ind') }}:</p>
    <ul class="list-unstyled inline list--comma">
        {% for tag in tags.getTags() %}
            <li><a data-bbc-container="test" href="{{ path('tag_show', {blogId: tags.getBlogId(), tagId: tag.getId()}) }}">
            {{- tag.getName() -}}
            </a></li>
        {% endfor %}
    </ul>
</div>
